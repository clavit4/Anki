'use strict';

/* ============================================================
   Boot — runs last, after every other script has loaded.
   ============================================================ */
renderDeckList();
